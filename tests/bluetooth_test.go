package test

import (
	"fmt"
	"testing"
)

func TestBluetooth(t *testing.T) {
	//adapterID := "hci1"
	//adapter, err := api.GetAdapter(adapterID)
	//if err != nil {
	//	log.Fatalf("Failed to get adapter: %s", err)
	//}
	//err = adapter.SetPowered(false)
	//if err != nil {
	//	log.Fatalf("Failed to enable adapter: %s", err)
	//}
	fmt.Println("hello")
}
